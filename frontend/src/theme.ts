import { createTheme } from "@mui/material/styles"

export const theme = createTheme({
  palette: {
    primary: {
      main: "#7f32cd",
    },
    secondary: {
      main: "#000000",
      light: "#ffffff",
      contrastText: "#13cdcd",
    },
  },
})
